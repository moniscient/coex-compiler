# Factorial Example
# Demonstrates: formula functions, recursion, conditionals

formula factorial(n: int) -> int
    if n <= 1
        return 1
    else
        return n * factorial(n - 1) ~~
 
func main() -> int
    for x in range(1,100)
    	print(factorial(x)) ~~
