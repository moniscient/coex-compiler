# Stress Test: String operations with 500k iterations
# Tests string allocation in loops

func main() -> int
    var count: int = 0
    for i in 0..500000
        var s: string = "hello"
        count = count + s.len()
    ~
    print(count)
    return 0
~
