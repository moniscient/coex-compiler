# Stress Test: Matrix (disabled - Matrix has implementation issues)
# Note: Matrix rows()/cols()/get() return 0 - implementation needs fixing

func main() -> int
    # Reduced size for now
    var mat: Matrix<int> = Matrix(100, 100, 0)

    # Set diagonal elements
    for i in 0..100
        mat = mat.set(i, i, i)
    ~

    print(mat.rows())
    print(mat.cols())
    return 0
~
