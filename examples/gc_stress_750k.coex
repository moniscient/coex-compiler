# GC Stress Test: 750k elements
# This test reliably triggers the GC stack overflow bug

func main() -> int
    var arr: List<int> = []
    var i: int = 0
    for i in 0..750000
        arr = arr.append(i)
    ~
    print(arr.len())
    print(arr.get(749999))
    return 0
~
